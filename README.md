AWS File Manager Frontend
Project Overview

This is the frontend application for the AWS File Manager project, built using Angular and TypeScript. The frontend interacts with a Spring Boot backend, which handles business logic, authentication, and communication with AWS S3 for file storage operations such as upload, download, rename, and delete.
The project is in development so the readme file wont give you a better picture 
will be updated soon 
